# EEAT评估系统 - 切换完整版需要做的事情

## 🚧 当前Vercel免费版的主要限制

### 1. **函数执行时间限制**
- **限制**: Vercel Hobby版本函数最长执行时间仅10秒
- **影响**: 复杂内容的AI评估经常超时，导致降级到规则评估
- **当前文件**: `vercel.json` (第9行)
```json
"maxDuration": 10
```

### 2. **AI模型调用超时**
- **限制**: 为配合10秒总限制，AI调用设置为5秒超时
- **影响**: 复杂文章无法得到充分的AI分析
- **当前文件**:
  - `lib/zhipu-evaluator.ts` (第66行)
  - `lib/ai-evaluator-v2.ts` (第69行)
  - `lib/eeat-evaluator.ts` (第54行)

### 3. **内容长度限制**
- **限制**: 为避免超时，建议内容不超过2500字符
- **影响**: 长文章评估质量下降
- **当前文件**: `app/page.tsx` (第171行)

### 4. **并发请求限制**
- **限制**: 免费版并发请求数量有限
- **影响**: 批量评估可能受限
- **当前文件**: `app/api/batch-evaluate/route.ts` (第23行)

## 🚀 完整版升级方案

### 阶段1: Vercel Pro版本升级

#### 1.1 更新Vercel配置
**文件**: `vercel.json`
```json
{
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 60  // 从10秒改为60秒
    }
  }
}
```

#### 1.2 增加AI评估超时时间
**文件**: `lib/zhipu-evaluator.ts`
```typescript
// 第66行
const timeoutId = setTimeout(() => controller.abort(), 30000); // 从5秒改为30秒

// 第74行
maxTokens: 8000, // 从3500增加到8000
```

**文件**: `lib/ai-evaluator-v2.ts`
```typescript
// 第69行
signal: AbortSignal.timeout(30000), // 从5秒改为30秒

// 第77行
max_tokens: 8000, // 从4000增加到8000
```

**文件**: `lib/eeat-evaluator.ts`
```typescript
// 第54行
{ timeout: 30000, maxRetries: 3 } // 从15000改为30000
```

#### 1.3 移除内容长度限制
**文件**: `app/page.tsx`
```typescript
// 删除或修改第171行的提示
// <li>• 内容长度限制：建议不超过 2500 字符</li>
// 改为：
// <li>• 支持长文章评估（建议5000字符以内获得最佳效果）</li>
```

### 阶段2: 功能增强

#### 2.1 优化AI评估质量
**文件**: `lib/eeat-prompt-v2.ts`
- 增加更详细的评估指令
- 添加特定行业的评估标准
- 优化输出格式要求

#### 2.2 增强批量处理能力
**文件**: `app/api/batch-evaluate/route.ts`
```typescript
// 第23行：增加批量限制
if (body.articles.length > 50) { // 从10改为50
  return NextResponse.json(
    { error: "批量评估最多支持50篇文章" },
    { status: 400 }
  )
}
```

#### 2.3 优化错误处理
**文件**: `lib/eeat-evaluator.ts`
- 添加更智能的重试机制
- 实现断路器模式（Circuit Breaker）
- 增加详细的错误分类和处理

#### 2.4 添加性能监控
**文件**: `lib/logger.ts`
```typescript
// 添加性能监控日志
export function logPerformance(operation: string, duration: number, details?: any) {
  logger.info(`Performance: ${operation}`, { duration, ...details });
}
```

### 阶段3: 用户体验优化

#### 3.1 移除限制提示
**文件**: `app/page.tsx`
- 移除固定通知横幅（第58-82行）
- 删除当前版本限制卡片（第132-151行）
- 更新Badge标签（第97-103行）

#### 3.2 增加评估选项
**文件**: `components/EvaluationForm.tsx`
- 添加评估深度选择（快速/标准/深度）
- 添加行业类型选择
- 增加评估报告格式选项

#### 3.3 优化结果展示
**文件**: `components/EvaluationResults.tsx`
- 添加可视化图表展示
- 增加详细分析展开/收起功能
- 添加导出PDF功能

### 阶段4: 高级功能

#### 4.1 历史记录功能
**新建文件**: `lib/storage.ts`
- 实现评估历史记录存储
- 添加历史记录查询API

**新建文件**: `app/api/history/route.ts`
- 历史记录查询接口
- 历史记录删除接口

#### 4.2 团队协作功能
**新建文件**: `app/api/team/route.ts`
- 团队成员管理
- 评估结果共享

#### 4.3 自定义评估模板
**新建文件**: `lib/templates.ts`
- 预定义行业评估模板
- 自定义评估标准

## 📋 升级检查清单

### 必须修改（立即生效）
- [ ] `vercel.json` - 升级Pro版本后修改maxDuration
- [ ] `lib/zhipu-evaluator.ts` - 增加超时时间和token限制
- [ ] `lib/ai-evaluator-v2.ts` - 增加超时时间和token限制
- [ ] `lib/eeat-evaluator.ts` - 增加整体超时时间
- [ ] `app/page.tsx` - 移除限制提示

### 建议修改（功能增强）
- [ ] `app/api/batch-evaluate/route.ts` - 增加批量处理限制
- [ ] `lib/eeat-prompt-v2.ts` - 优化AI评估指令
- [ ] `lib/logger.ts` - 添加性能监控
- [ ] `components/EvaluationForm.tsx` - 增加评估选项
- [ ] `components/EvaluationResults.tsx` - 优化结果展示

### 新增功能（可选）
- [ ] `lib/storage.ts` - 历史记录存储
- [ ] `app/api/history/route.ts` - 历史记录API
- [ ] `app/api/team/route.ts` - 团队协作API
- [ ] `lib/templates.ts` - 自定义模板

## 💰 成本考虑

### Vercel Pro版本 ($20/月)
- 函数执行时间：从10秒增加到60秒
- 带宽：从100GB增加到1TB
- 并发请求：无限制

### AI API成本
- 智谱AI：约 ¥0.005/千tokens
- OpenAI：约 $0.03/千tokens
- 预估每月评估1000篇文章：¥50-200

## 🎯 升级优先级

1. **高优先级**：升级Vercel Pro + 修改超时配置（立即解决核心问题）
2. **中优先级**：优化AI评估质量 + 移除用户限制（提升用户体验）
3. **低优先级**：添加高级功能（增加产品竞争力）

---

**注意**: 升级前请确保：
1. 已备份当前代码
2. 测试环境已验证所有更改
3. API密钥余额充足
4. 用户已收到升级通知